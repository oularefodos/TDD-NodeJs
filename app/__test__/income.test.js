const request = require('supertest');
const app = require('../app');

describe('Income API', () => {

    it('GET /Income --> array incomes', () => {})

    it('GET /Income/id --> a income with the same id', () => {}) 
    it('GET /Income/id --> nothing', () => {})

    it('POST /income --> created income', () => {})

})