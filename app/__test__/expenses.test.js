const request = require('supertest');
const app = require('../app');

describe('Expense API', () => {

    it('GET /Expenses --> array expenses', () => {})

    it('GET /Expenses/id --> a expense with the same id', () => {})

    it('GET /Expenses/id --> nothing', () => {})

    it('POST /expense --> created expense', () => {})

})